name: my first pipeline

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
        - name: Checkout repository code
          uses: actions/checkout@v4
        - name: Build image
          run: docker build -t malvinerye/cipotfolio .
        - name: List images  
          run: docker images
           

    


 